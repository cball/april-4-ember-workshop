import { expect } from 'chai';
import { describe, it } from 'mocha';
import loadSlow from 'repo-browser/utils/load-slow';

describe('Unit | Utility | load slow', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = loadSlow();
    expect(result).to.be.ok;
  });
});
