import loadSlow from 'repo-browser/utils/load-slow';
import { module, test } from 'qunit';

module('Unit | Utility | load slow');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = loadSlow();
  assert.ok(result);
});
